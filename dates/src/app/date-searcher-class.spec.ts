import { DateSearcherClass } from './date-searcher-class';

describe('DateSearcherClass', () => {
  it('should create an instance', () => {
    expect(new DateSearcherClass()).toBeTruthy();
  });
});
